# BE Data is pure-compute; env is minimal.
PORT=8000
# Max single-token exposure cap by risk tolerance (used by RASA).
RASA_MAX_EXPOSURE_LOW=0.30
RASA_MAX_EXPOSURE_MED=0.40
RASA_MAX_EXPOSURE_HIGH=0.50
